# Security settings for npm
audit-level=moderate
fund=false
save-exact=true

# Performance settings
prefer-offline=true
progress=false

# Fix peer dependency conflicts (React 19 + testing-library)
legacy-peer-deps=true